[tool.ruff]
target-version = "py39"
line-length = 100

exclude = [
  ".git",
  ".venv",
  "orthanc-db",
  "simulator-data",
]

[tool.ruff.lint]
# Keep this intentionally small: focus on real defects and "unused" signals.
select = ["F", "E9", "W6"]
